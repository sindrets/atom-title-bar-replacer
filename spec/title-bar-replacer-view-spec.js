'use babel';

import TitleBarReplacerView from '../lib/title-bar-replacer-view';

describe('TitleBarReplacerView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
